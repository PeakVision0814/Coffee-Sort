# --- 硬件通信 ---
PORT = "COM3"           # 明天现场确认端口
BAUD = 115200           # myCobot 280 固定波特率

# --- 关键高度 (单位: mm) ---
# 1. 观测高度：必须与手眼标定时的 TEST_Z 保持一致！(非常重要)
OBSERVE_Z = 200

# 2. 安全移动高度：抓完东西后，提起来的高度，防止撞到其他东西
SAFE_Z = 215

# 3. 抓取高度：夹爪闭合时的高度（桌面高度 + 物体高度偏差）
# 明天需要微调这个值，太高抓不到，太低会撞桌子
PICK_Z = 90.0           

# --- 关键点位 (角度 list: [j1, j2, j3, j4, j5, j6]) ---
# 观测姿态：机械臂高高抬起，摄像头垂直向下看
OBSERVE_POSE = [-5.27, 5.71, -96.85, -0.79, 1.05, 138.69]

# 待机/安全姿态
HOME_POSE = [0, 0, 0, 0, 0, 0]

# --- 仓库槽位配置 (6槽布局) ---
# 结构: { 槽位ID: [x, y, z, rx, ry, rz] }
# 说明: z 高度通常一致，x/y 根据实际位置填
# 目前先填入虚拟数据，等硬件到了用 tool_get_coords.py 测量
STORAGE_RACKS = {
    1: [-150,  150, 104.5, -177, -2, 39],  # 假设 1 号
    2: [-150,   50, 104.5, -177, -2, 39],  # 假设 2 号
    3: [-150,  -50, 104.5, -177, -2, 39],  # ...
    4: [-150, -150, 104.5, -177, -2, 39],
    5: [-250,   50, 104.5, -177, -2, 39],  # 第二排
    6: [-250,  -50, 104.5, -177, -2, 39]
}

# --- 🆕 视觉识别参数 ---
# 定义“最近”的阈值：Y轴越大越靠前（假设机械臂前方是Y+，具体看实际坐标系）
# 这里不需要改，逻辑写在 vision.py 里即可