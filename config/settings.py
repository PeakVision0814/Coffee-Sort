# --- 硬件通信 ---
PORT = "COM3"           # 明天现场确认端口
BAUD = 115200           # myCobot 280 固定波特率

# --- 关键高度 (单位: mm) ---
# 1. 观测高度：必须与手眼标定时的 TEST_Z 保持一致！(非常重要)
OBSERVE_Z = 200.0       

# 2. 安全移动高度：抓完东西后，提起来的高度，防止撞到其他东西
SAFE_Z = 220.0          

# 3. 抓取高度：夹爪闭合时的高度（桌面高度 + 物体高度偏差）
# 明天需要微调这个值，太高抓不到，太低会撞桌子
PICK_Z = 90.0           

# --- 关键点位 (角度 list: [j1, j2, j3, j4, j5, j6]) ---
# 观测姿态：机械臂高高抬起，摄像头垂直向下看
OBSERVE_POSE = [0, -10, -120, 40, 0, 0]  

# 待机/安全姿态
HOME_POSE = [0, 0, 0, 0, 0, 0]

# --- 仓库位置 (坐标 list: [x, y, z, rx, ry, rz]) ---
# 假设 A 仓在左边，B 仓在右边 (明天现场用 mc.get_coords() 测一下填进去)
# 这里的 rx,ry,rz 保持 -180, 0, 0 (垂直向下)
BIN_A_COORDS = [150.0, 150.0, 150.0, -180, 0, 0]  
BIN_B_COORDS = [150.0, -150.0, 150.0, -180, 0, 0]